eureka:
  instance:
    preferIpAddress: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://172.18.0.4:8761/eureka
spring:
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: yeonon
    password: 124563
    url: jdbc:mysql://172.17.197.203:3306/sca?useSSL=false

  cloud:
    
    stream:
      bindings:
        output:
          destination: orgChangeTopic
          content-type: application/json
          binder: remote-rabbit
      binders:
        remote-rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: 172.17.197.203
                port: 5672
                username: admin
                password: 124563    

security:
  oauth2:
    resource:
      user-info-uri: http://172.18.0.7:3000/user
name: dev
